ALTER TABLE GUEST 
DROP CONSTRAINT GUEST_PK;

CREATE TABLE GUEST 
(
  ID NUMBER NOT NULL 
, NAME VARCHAR2(40 BYTE) NOT NULL 
, BIRTH_DATE DATE NOT NULL 
, BIRTH_CITY VARCHAR2(40 BYTE) NOT NULL 
, TAX_NUMBER NUMBER NOT NULL 
, SSN VARCHAR2(10 BYTE) NOT NULL 
, ADDRESS VARCHAR2(50 BYTE) NOT NULL 
, SEX CHAR(1 BYTE) NOT NULL 
, ROOM NUMBER NOT NULL 
, CHECK_IN DATE NOT NULL 
, CHECK_OUT DATE NOT NULL 
, CONSTRAINT GUEST_PK PRIMARY KEY 
  (
    ID 
  )
  ENABLE 
) 
LOGGING 
TABLESPACE USERS 
PCTFREE 10 
INITRANS 1 
STORAGE 
( 
  INITIAL 65536 
  NEXT 1048576 
  MINEXTENTS 1 
  MAXEXTENTS UNLIMITED 
  BUFFER_POOL DEFAULT 
) 
NOPARALLEL;

ALTER TABLE GUEST
ADD CONSTRAINT GUEST_PK PRIMARY KEY 
(
  ID 
)
ENABLE;

CREATE SEQUENCE GUEST_SEQ1;

CREATE TRIGGER GUEST_TRG1 
BEFORE INSERT ON GUEST 
FOR EACH ROW 
BEGIN
  <<COLUMN_SEQUENCES>>
  BEGIN
    IF INSERTING AND :NEW.ID IS NULL THEN
      SELECT GUEST_SEQ1.NEXTVAL INTO :NEW.ID FROM SYS.DUAL;
    END IF;
  END COLUMN_SEQUENCES;
END;
/
